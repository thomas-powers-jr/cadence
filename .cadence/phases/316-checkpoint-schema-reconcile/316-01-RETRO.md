# Retro

## Code review findings

- packages/checkpoint/src/validate.ts: MEDIUM — A BOM-prefixed string skips frontmatter detection, so valid v1 handoffs are treated as v2 and wrongly fail for missing Open decisions. Strip a leading BOM before parsing. (line 80)
